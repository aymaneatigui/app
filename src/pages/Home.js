import { useQuery } from "@tanstack/react-query";
import axios from "axios";

export const Home = () => {

  const {} = useQuery(["cat"],)

  return (
    <h1>
      Home Page<p></p>
    </h1>
  );
};
